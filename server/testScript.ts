import Tag from './models/Tag'

export default async () => {
  // const JASON_UID_PROD = '60b43675702bb67d157913bc'
  // const JASON_UID_DEV = '60b056530dd0935cd6c44fb4'
  // const TAG_ID = '60ed972bdf0cd909b639550c'
  // await Tag.findByIdAndDelete(TAG_ID)
  // console.log('done')
  // const task = await Task.findOneAndUpdate(
  //   { userId: JASON_UID_DEV, isComplete: true },
  //   { isComplete: false },
  //   { new: true }
  // ).sort({ updatedAt: -1 })
  // const promises = tasks.map(async (task) => {
  //   task.userId = jasonUserId
  //   await task.save()
  // })
  // await Promise.all(promises)
  // console.log('userId update complete')
}
