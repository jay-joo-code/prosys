import Task from './models/Task'
import User from './models/User'

export default async () => {
  // const users = await User.find()
  // const tasks = await Task.find({ userId: '60a9fa062bfdca0004875ece' })
  // const jasonUserId = '60b43675702bb67d157913bc'
  // const promises = tasks.map(async (task) => {
  //   task.userId = jasonUserId
  //   await task.save()
  // })
  // await Promise.all(promises)
  // console.log('userId update complete')
}
